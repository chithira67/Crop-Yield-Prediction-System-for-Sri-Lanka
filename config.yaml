# Configuration file for Crop Yield Prediction System

# Data paths
data:
  raw_path: "data/raw/crop_yield_data.csv"
  processed_path: "data/processed/features.csv"
  
# Model configuration
model:
  test_size: 0.2
  random_state: 42
  models_dir: "model"
  
  # Hyperparameter tuning
  hyperparameter_tuning:
    n_iter: 20
    cv: 5
    n_jobs: -1
    
  # Random Forest parameters
  random_forest:
    n_estimators: [100, 200, 300]
    max_depth: [10, 20, 30, null]
    min_samples_split: [2, 5, 10]
    min_samples_leaf: [1, 2, 4]
    
  # XGBoost parameters
  xgboost:
    n_estimators: [100, 200, 300]
    max_depth: [3, 5, 7, 10]
    learning_rate: [0.01, 0.1, 0.2]
    subsample: [0.8, 0.9, 1.0]
    colsample_bytree: [0.8, 0.9, 1.0]
    
  # LightGBM parameters
  lightgbm:
    n_estimators: [100, 200, 300]
    max_depth: [3, 5, 7, 10]
    learning_rate: [0.01, 0.1, 0.2]
    subsample: [0.8, 0.9, 1.0]
    colsample_bytree: [0.8, 0.9, 1.0]
    num_leaves: [31, 50, 100]

# Feature engineering
features:
  min_crop_years: 3
  lag_periods: [1, 2]
  rolling_window: 3
  
# Logging
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file: "logs/crop_yield_prediction.log"
  
# Streamlit app
app:
  title: "Crop Yield Prediction System - Sri Lanka"
  page_icon: "🌾"
  layout: "wide"



