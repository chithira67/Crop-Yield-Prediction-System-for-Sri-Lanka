from setuptools import setup, find_packages

setup(
    name="crop-yield-system",
    version="0.1",
    description="Crop Yield Prediction System for Sri Lanka",
    packages=find_packages(),
    python_requires=">=3.8",
)

